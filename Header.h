//create a header file to declare all my functions
int print_all(char *studentArray, int buffSize);
int print_one_data(char *studentArray, int buffSize);
int print_just_one(char *studentArray, int buffSize);